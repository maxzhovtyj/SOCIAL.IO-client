import React from 'react';

const ProfileInfo = (props) => (
    <div>
        <img src="" alt="userImg"/>
        <div>
            ava + description
        </div>
    </div>
);

export default ProfileInfo;